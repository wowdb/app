const size = {
  title: 26,
  subtitle: 18
}

const weight = {
  normal: '400',
  semibold: '600',
  bold: 'bold'
}

export default {
  size,
  weight,

  title: {
    fontSize: size.title,
    fontWeight: weight.semibold
  },
  subtitle: {
    fontSize: size.subtitle,
    fontWeight: weight.semibold
  }
}
